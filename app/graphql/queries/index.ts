import notificationSchema from './notification_schema';
import patientSchema from './patient_schema';
import questionSchema from './question_schema';


const schema = [
  patientSchema,
  questionSchema,
  notificationSchema
];

export default schema;
